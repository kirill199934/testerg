# 🚀 Настройка админ-панели для SkyNodes.net

## Шаг 1: Настройка RCON на SkyNodes

### Через панель управления SkyNodes:

1. **Войдите в панель** SkyNodes.net
2. **Откройте ваш сервер** → "Управление сервером"
3. **Найдите раздел "Файлы"** или "File Manager"
4. **Откройте файл `server.properties`**
5. **Найдите и измените следующие строки:**

```properties
# Если строк нет - добавьте их
enable-rcon=true
rcon.port=25575
rcon.password=ВАШ_БЕЗОПАСНЫЙ_ПАРОЛЬ
enable-query=true
query.port=25565
```

6. **Сохраните файл**
7. **Перезапустите сервер** через панель управления

### Альтернативный способ (через консоль панели):
Если есть консоль в панели SkyNodes, можете выполнить:
```
stop
# дождитесь остановки, затем измените server.properties и запустите снова
```

## Шаг 2: Узнаем данные вашего сервера

Мне нужны следующие данные из панели SkyNodes:

### 📋 Данные сервера:
- **IP адрес сервера:** `____________` (обычно типа mc.skynodes.net или IP)
- **Порт сервера:** `____________` (обычно 25565 или другой)
- **RCON пароль:** `____________` (который вы установили выше)

### 🔍 Где найти IP и порт:
В панели SkyNodes обычно есть раздел:
- "Информация о сервере"
- "Server Info" 
- "Подключение"

Там будет указан адрес типа:
- `mc.skynodes.net:25565` или
- `123.45.67.89:25565`

## Шаг 3: Создание .env файла

После получения данных, создам вам готовый .env файл с вашими настройками.

## Возможные особенности SkyNodes:

### ✅ Что обычно работает:
- RCON подключение
- Query статистика
- Стандартные команды сервера

### ⚠️ Возможные ограничения:
- Некоторые хостинги блокируют внешние RCON подключения
- Может потребоваться разрешение в панели управления
- Некоторые команды могут быть ограничены

### 💡 Если RCON не работает:
1. Проверьте в панели SkyNodes настройки безопасности
2. Убедитесь что RCON не заблокирован файерволом
3. Попробуйте использовать только Query (ограниченный функционал)

## Шаг 4: Тестирование

После настройки запустим тест:
```bash
cd api
npm run test
```

Это покажет что работает, а что нужно дополнительно настроить.