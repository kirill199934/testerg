# TravHouse API Simple Plugin

Упрощенная версия плагина для быстрого тестирования.

## 🚀 Быстрый старт

### Вариант 1: Автоматическая сборка
```bash
# 1. Скачать Spigot JAR
download-spigot.bat

# 2. Собрать плагин  
build-simple.bat
```

### Вариант 2: Ручная сборка

1. **Скачайте Spigot JAR** с https://getbukkit.org/download/spigot
2. **Поместите JAR файл** в папку `simple-plugin/`
3. **Запустите компиляцию:**
   ```bash
   javac -cp "spigot-1.20.1.jar" TravHouseAPIPlugin.java
   ```
4. **Создайте JAR архив:**
   ```bash
   jar cf TravHouseAPI.jar *.class plugin.yml config.yml
   ```

### Вариант 3: Готовый плагин для тестирования

Если сборка не работает, можете:
1. Скачать готовый плагин из релизов
2. Или использовать только веб-часть админ-панели

## 📁 Файлы плагина

- `TravHouseAPIPlugin.java` - основной код плагина
- `plugin.yml` - описание плагина для Bukkit
- `config.yml` - конфигурация (порт API)

## 🔧 API Endpoints

После установки плагина будут доступны:
- `http://localhost:8080/api/status` - статус сервера
- `http://localhost:8080/api/players` - список игроков
- `http://localhost:8080/api/performance` - производительность
- `http://localhost:8080/api/health` - проверка работы

## 🎮 Команды в игре

- `/thapi` - информация о плагине
- `/thapi status` - статус API сервера
- `/thapi reload` - перезагрузка конфигурации

## ⚡ Преимущества

- ✅ **Простота** - один файл Java
- ✅ **Не требует Maven** - компиляция javac
- ✅ **Минимальные зависимости** - только Bukkit API
- ✅ **Быстрая установка** - скопировать JAR в plugins/

## 🐛 Если не работает

1. **Проверьте Java версию:** `java -version` (нужна 17+)
2. **Проверьте порт:** убедитесь что 8080 не занят
3. **Проверьте логи сервера** на ошибки плагина
4. **Протестируйте API:** `curl http://localhost:8080/api/health`

## 🔄 Интеграция с админ-панелью

Файл `admin-connected.js` уже настроен на порт 8080. После установки плагина админ-панель должна заработать автоматически.