# 🚀 Инструкция по подключению админ-панели к серверу

## 📋 Что мы создали

1. **Backend API** (`api/server.js`) - сервер для связи с Minecraft
2. **Подключенная админ-панель** (`admin-panel-connected.html`) - новая версия с реальным подключением
3. **JavaScript клиент** (`admin-connected.js`) - логика подключения к API
4. **Конфигурация** (`api/package.json`, `api/.env.example`) - настройки проекта

## 🔧 Шаг 1: Настройка Minecraft сервера

### Включите RCON на вашем сервере:
Откройте файл `server.properties` на вашем Minecraft сервере и добавьте/измените:

```properties
enable-rcon=true
rcon.port=25575
rcon.password=ВАШ_БЕЗОПАСНЫЙ_ПАРОЛЬ
enable-query=true
query.port=25565
```

**Важно**: После изменений перезапустите сервер!

## 🔧 Шаг 2: Установка Backend API

### 1. Установите Node.js
- Скачайте с https://nodejs.org/ (версия 16+)
- Проверьте установку: `node --version`

### 2. Установите зависимости
```bash
cd api
npm install
```

### 3. Создайте файл конфигурации
Скопируйте `.env.example` в `.env` и настройте:

```env
MC_HOST=IP_ВАШЕГО_СЕРВЕРА
MC_PORT=25565
RCON_PORT=25575
RCON_PASSWORD=ВАШ_RCON_ПАРОЛЬ
QUERY_PORT=25565
PORT=3000
```

**Пример:**
```env
MC_HOST=123.45.67.89
MC_PORT=25565
RCON_PORT=25575
RCON_PASSWORD=MySecurePassword123
QUERY_PORT=25565
PORT=3000
```

## 🚀 Шаг 3: Запуск

### 1. Запустите API сервер
```bash
cd api
npm start
```

### 2. Откройте админ-панель
Откройте `admin-panel-connected.html` в браузере

## 🔍 Шаг 4: Проверка подключения

### Проверьте API:
Откройте в браузере: `http://localhost:3000/api/health`

Должен вернуть:
```json
{
  "status": "OK",
  "timestamp": "2024-12-...",
  "server_config": {
    "host": "ваш-ip",
    "port": 25565,
    "rcon_port": 25575
  }
}
```

### Проверьте статус сервера:
`http://localhost:3000/api/status`

## 🛠️ Возможные проблемы и решения

### ❌ "RCON недоступен"
- **Проверьте:** включен ли RCON в `server.properties`
- **Проверьте:** правильный ли пароль RCON
- **Проверьте:** открыт ли порт RCON (25575) в файерволе
- **Попробуйте:** перезапустить Minecraft сервер

### ❌ "Connection refused" / "ECONNREFUSED"
- **Проверьте:** правильный ли IP адрес сервера
- **Проверьте:** работает ли сервер
- **Проверьте:** доступен ли сервер из сети (ping IP)

### ❌ "Timeout" ошибки
- **Проверьте:** нет ли блокировки файерволом
- **Попробуйте:** увеличить timeout в `admin-connected.js`

### ❌ API не отвечает
- **Проверьте:** запущен ли Node.js сервер
- **Проверьте:** правильный ли порт (по умолчанию 3000)
- **Посмотрите:** логи в консоли Node.js

## 📊 Что работает после подключения

### ✅ Доступные функции:
- 📊 **Статус сервера** - онлайн/офлайн, игроки
- 👥 **Список игроков** - кто онлайн
- ⌨️ **Выполнение команд** - любые команды сервера
- 🚫 **Кик/бан игроков** - модерация
- 💾 **Сохранение мира** - принудительное сохранение
- 🛑 **Остановка сервера** - безопасная остановка
- 📢 **Сообщения всем** - broadcast сообщения
- 📈 **TPS мониторинг** - производительность сервера

### 🔄 Автообновление:
- Статистика обновляется каждые 30 секунд
- Список игроков обновляется каждую минуту
- Консоль работает в реальном времени

## 🌐 Деплой в продакшн

### Для развертывания на хостинге:

1. **Загрузите все файлы** на ваш веб-хостинг
2. **Установите Node.js** на сервере (если поддерживается)
3. **Настройте переменные окружения** на хостинге
4. **Измените API_CONFIG.baseURL** в `admin-connected.js`:
   ```javascript
   const API_CONFIG = {
       baseURL: 'https://ваш-домен.com/api', // Вместо localhost
       timeout: 10000,
       retryAttempts: 3
   };
   ```

### Альтернативы для хостинга без Node.js:
- **Vercel** (бесплатно) - поддерживает Node.js API
- **Netlify Functions** (бесплатно) - serverless функции
- **Heroku** (платно) - полная поддержка Node.js
- **Railway** (бесплатный лимит) - простой деплой

## 🔐 Безопасность

### Рекомендации:
1. **Используйте сложный RCON пароль** (20+ символов)
2. **Ограничьте доступ к API** (только для админов)
3. **Используйте HTTPS** в продакшене
4. **Закройте RCON порт** для внешних подключений
5. **Регулярно обновляйте** зависимости npm

## 📞 Поддержка

Если что-то не работает:

1. **Проверьте логи** Node.js сервера
2. **Откройте Developer Tools** в браузере (F12)
3. **Проверьте Network** вкладку для API запросов
4. **Попробуйте** прямые API запросы через браузер

---

**Готово!** 🎉 Ваша админ-панель теперь подключена к серверу и готова к использованию!