# 🤖 Установка и запуск TravHouse Telegram Bot

## 📦 Установка зависимостей

### 1. Установите Python 3.8+
```bash
# Windows - скачайте с python.org
# Linux/Mac
sudo apt update && sudo apt install python3 python3-pip
```

### 2. Установите библиотеки
```bash
pip install -r requirements.txt
```

## ⚙️ Настройка

### 1. Настройте конфиг в `telegram_bot.py`:
```python
BOT_TOKEN = "8341142360:AAFm7SN7n3ZIjxZpyouCk-ksL1Bv6vDM9tY"
ADMIN_IDS = [7740147216, ВТОРОЙ_АДМИН_ID]  # Добавьте всех админов
```

### 2. (Опционально) Создайте config.py:
```python
# config.py
BOT_TOKEN = "ваш_токен"
ADMIN_IDS = [7740147216, 123456789]
```

## 🚀 Запуск

```bash
python telegram_bot.py
```

## 🎮 Функциональность бота

### 📋 **Основные команды:**
- `/start` - Главное меню с кнопками
- `/stats` - Статистика сервера в реальном времени
- `/players` - Список игроков онлайн
- `/rules` - Правила сервера
- `/ip` - IP для подключения (Java/Bedrock)
- `/help` - Полная справка

### ⚙️ **Админские команды:**
- `/admin` - Панель администратора
- `/applications` - Управление заявками
- `/broadcast` - Рассылка всем пользователям
- Inline кнопки для одобрения/отклонения заявок

### 🔧 **Интерактивные функции:**
- **Inline меню** с красивыми кнопками
- **Автообработка заявок** с сайта с админскими кнопками
- **Статистика в реальном времени**
- **Система уведомлений**
- **Многоуровневое меню**

## 📱 **Что умеет бот:**

### Для игроков:
✅ Показывает статистику сервера
✅ Список онлайн игроков  
✅ Правила и IP сервера
✅ Ссылки на сайт и соцсети
✅ Новости и обновления

### Для админов:
✅ Обрабатывает заявки с кнопками одобрить/отклонить
✅ Статистика заявок и пользователей
✅ Рассылка сообщений всем
✅ Управление настройками
✅ Просмотр логов

## 🛠️ **Продвинутые настройки:**

### Подключение к API Minecraft сервера:
```python
# В функции получения статистики замените на реальный API
async def get_server_stats():
    # Подключение к API вашего сервера
    response = requests.get("http://your-server.com/api/stats")
    return response.json()
```

### База данных для заявок:
```python
import sqlite3

# Создание БД для хранения заявок
def init_database():
    conn = sqlite3.connect('travhouse.db')
    cursor = conn.cursor()
    cursor.execute('''
        CREATE TABLE applications (
            id INTEGER PRIMARY KEY,
            name TEXT,
            nickname TEXT, 
            age INTEGER,
            telegram TEXT,
            status TEXT,
            created_at TIMESTAMP
        )
    ''')
    conn.commit()
    conn.close()
```

## 🔄 **Автозапуск (Linux):**

### 1. Создайте systemd сервис:
```bash
sudo nano /etc/systemd/system/travhouse-bot.service
```

### 2. Содержимое файла:
```ini
[Unit]
Description=TravHouse Telegram Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/travhouse
ExecStart=/usr/bin/python3 telegram_bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

### 3. Запустите сервис:
```bash
sudo systemctl enable travhouse-bot
sudo systemctl start travhouse-bot
sudo systemctl status travhouse-bot
```

## 📊 **Мониторинг:**

```bash
# Просмотр логов
journalctl -u travhouse-bot -f

# Перезапуск
sudo systemctl restart travhouse-bot
```

## 🆘 **Решение проблем:**

### Ошибка импорта:
```bash
pip install --upgrade python-telegram-bot
```

### Ошибка токена:
- Проверьте правильность токена
- Убедитесь что бот не заблокирован

### Ошибка прав админа:
- Добавьте ваш Telegram ID в ADMIN_IDS
- Узнать ID: напишите @userinfobot

## 📞 **Поддержка:**
- GitHub Issues
- Telegram: @your_support_username